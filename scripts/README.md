# scripts

Helper scripts for local development and ops.

Planned:
- gen_sodium_key.py : generate a random libsodium secret key (hex) for local encryption
- dev_seed.sh       : create local storage buckets / seed test data

