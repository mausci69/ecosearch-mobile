# infra

Infrastructure and orchestration for EcoSearch.
This folder contains configuration for:

- docker-compose.yml
- reverse proxy (Caddy or Nginx)
- local Postgres, Redis, and MinIO services

> Next steps (in separate commits):
> - Add docker-compose.yml with core services
> - Add proxy config (Caddyfile or nginx.conf)

